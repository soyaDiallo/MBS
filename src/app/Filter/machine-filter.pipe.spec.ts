import { MachineFilterPipe } from './machine-filter.pipe';

describe('MachineFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MachineFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
