import { ErreurFilterPipe } from './erreur-filter.pipe';

describe('ErreurFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ErreurFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
