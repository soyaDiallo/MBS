import { ReparationFilterPipe } from './reparation-filter.pipe';

describe('ReparationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReparationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
