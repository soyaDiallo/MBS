import { PieceFilterPipe } from './piece-filter.pipe';

describe('PieceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PieceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
