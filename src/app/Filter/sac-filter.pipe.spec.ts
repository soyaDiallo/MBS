import { SacFilterPipe } from './sac-filter.pipe';

describe('SacFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SacFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
