import { ClientFilterPipe } from './client-filter.pipe';

describe('ClientFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
